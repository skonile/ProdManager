<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | ProdManager</title>
    <link rel="stylesheet" href="/asserts/css/main.css">
    <link rel="stylesheet" href="/asserts/css/formbuilder.css">
    <style>
        .login-cont{
            width: 100%;
            height: 100vh;
            background-color: rgba(0, 0, 0, .05);
            overflow: auto;
        }

        .login-cont-inner{
            width: 350px;
            margin: 20vh auto;
            background-color: #fff;
            border-radius: 5px;
            overflow: hidden;
            padding: 20px;
        }

        .login-cont img{
            width: 200px;
            margin-left: 55px;
        }

        .login-cont input{
            border: 1px solid #ccc;
        }

        .login-cont input[type='submit']{
            border: none;
            background-color: var(--primary-color);
            color: #fff;
            cursor: pointer;
        }

        .login-cont input[type='submit']:hover{
            background-color: var(--primary-hover-color);
        }
    </style>
</head>
<body>
    <div class="login-cont">
        <div class="login-cont-inner">
            <div class="logo-cont">
                <img src="/asserts/img/logo/logo.png" alt="LOGO" class="logo">
            </div>
            <form method="post" action="/login" class="login-form">
                <div class="form-error {% if errors %}formbuilder-errors{% endif %}">
                {% if errors %}
                    <ul>
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                    </ul>
                {% endif %}
                </div>
                <div class="form-row">
                    <input type="text" name="username" id="username" placeholder="Username" value="{{ username }}">
                </div>
                <div class="form-row">
                    <input type="password" name="password" id="password" placeholder="Password">
                </div>
                <div class="form-row">
                    <input type="submit" value="Login">
                </div>
            </form>
        </div>
    </div>
</body>
</html>